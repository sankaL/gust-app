{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.railway"
  },
  "deploy": {
    "preDeployCommand": "sh -lc 'if [ \"${APP_ENV}\" = \"production\" ] && [ -z \"${MIGRATION_DATABASE_URL}\" ]; then echo \"MIGRATION_DATABASE_URL is required for production Alembic deploys.\"; exit 1; fi; alembic upgrade head'",
    "startCommand": "sh -lc 'uvicorn app.main:app --host 0.0.0.0 --port ${PORT}'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 5
  }
}

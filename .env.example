# Gust environment reference.
# Copy this to `.env` for local work. Use `.env.prod` as a production-oriented template.
# Google OAuth client secrets are configured in Supabase, not in Gust app env files.

# Backend startup essentials
APP_ENV=development
GUST_DEV_MODE=true
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/postgres
LOCAL_DEV_AUTH_SECRET=replace-with-at-least-32-random-characters
MIGRATION_DATABASE_URL=
REQUIRED_ALEMBIC_REVISION=0019_pushover_reminders
RUN_STARTUP_CHECKS=true
LOG_LEVEL=INFO

# Auth and app URLs
# Production uses Supabase Auth. The lean local stack leaves these unset and uses the
# dev-only signed session secret above.
FRONTEND_APP_URL=http://localhost:3000
BACKEND_PUBLIC_URL=http://localhost:8000
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Session and retention settings
SESSION_COOKIE_SECURE=false
SESSION_COOKIE_DOMAIN=
CAPTURE_RETENTION_DAYS=7

# Abuse controls (limit/window-seconds, comma-separated for multiple windows)
RATE_LIMIT_PUBLIC_GET_IP=120/60
RATE_LIMIT_UNAUTHENTICATED_WRITE_IP=60/60,600/3600
RATE_LIMIT_INTERNAL_JOB_IP=10/60,100/3600

# Transcription provider: Mistral
MISTRAL_API_URL=https://api.mistral.ai/v1/audio/transcriptions
MISTRAL_API_KEY=
MISTRAL_TRANSCRIPTION_MODEL=voxtral-mini-latest
TRANSCRIPTION_TIMEOUT_SECONDS=20

# Extraction provider: OpenRouter
OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions
OPENROUTER_API_KEY=
OPENROUTER_EXTRACTION_MODEL=openai/gpt-5.6-luna
EXTRACTION_TIMEOUT_SECONDS=20

# Reminder delivery: Resend
RESEND_API_URL=https://api.resend.com/emails
RESEND_API_KEY=
RESEND_FROM_EMAIL=
INTERNAL_JOB_SHARED_SECRET=
REMINDER_BATCH_SIZE=50
REMINDER_CLAIM_TIMEOUT_SECONDS=600
REMINDER_REQUEST_TIMEOUT_SECONDS=10

# Frontend build-time env
VITE_GUST_DEV_MODE=true
VITE_API_BASE_URL=http://localhost:8000
# Contact email shown on the landing page for access requests.
VITE_ADMIN_EMAIL=sanka.lokuliyana@gmail.com
